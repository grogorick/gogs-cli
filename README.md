# Gogs command line util

Interact with Gogs servers via *curl* requests to the *REST API v1*.

Use `./gogs` to show available actions.  
Use `./gogs <some-action>` to show required parameter syntax.
